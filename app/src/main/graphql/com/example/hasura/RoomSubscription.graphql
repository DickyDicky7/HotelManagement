subscription RoomSubscription
{
    ROOM( order_by: [ { name: asc_nulls_last } ] )
    {
        id
        name
        note
        created_at
        updated_at
        description
        is_occupied
        roomkind_id
    }
}

