subscription GuestSubscription
{
    GUEST( order_by: [ { name: asc_nulls_last } ] )
    {
        id
        name
        address
        id_number
        created_at
        updated_at
        phone_number
        guestkind_id
    }
}

