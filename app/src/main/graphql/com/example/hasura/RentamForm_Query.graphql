query RentalForm_All{
    RENTALFORM {
        amount
        bill_id
        created_at
        guest_id
        id
        is_resolved
        number_of_guests
        price_per_day
        rental_days
        room_id
        start_date
        updated_at
    }
}