mutation BillInsert
(
    $cost   : Float
    $isPaid : Boolean
    $guestId: Int
)
{
    insert_BILL
    (
        objects:
        [
            {
                cost    : $cost
                is_paid : $isPaid
                guest_id: $guestId
            }
        ]
    )
    {
        returning
        {
            id
            cost
            is_paid
            guest_id
            created_at
            updated_at
        }
    }
}
