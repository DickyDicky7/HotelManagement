query ROOMKIND_By_Id_Query($id: Int) {
    ROOMKIND(where: {id: {_eq: $id}}) {
        id
        name
        price
        capacity
        surcharge_percentage
        created_at
        updated_at
    }
}