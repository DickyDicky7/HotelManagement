subscription RoomKindSubscription
{
    ROOMKIND( order_by: [ { area: asc_nulls_last }, { price: asc_nulls_last }, { capacity: asc_nulls_last }, { number_of_beds: asc_nulls_last } ] )
    {
        id
        name
        area
        price
        capacity
        image_url
        created_at
        updated_at
        number_of_beds
        surcharge_percentage
    }
}

