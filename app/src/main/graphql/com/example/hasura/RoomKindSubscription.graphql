subscription RoomKindSubscription
{
    ROOMKIND( order_by: [ { name: asc_nulls_last } ] )
    {
        id
        name
        area
        price
        capacity
        image_url
        created_at
        updated_at
        number_of_beds
        surcharge_percentage
    }
}

