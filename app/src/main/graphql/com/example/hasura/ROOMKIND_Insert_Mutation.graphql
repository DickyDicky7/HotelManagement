mutation ROOMKIND_Insert_Mutation
(
    $name: String,
    $price: money,
    $capacity: smallint,
    $surcharge_percentage: Float,
    $surcharge_start_ordinal: smallint,
)
{
    insert_ROOMKIND
    (objects:
    [
        {
            name: $name,
            price: $price,
            capacity: $capacity,
            surcharge_percentage: $surcharge_percentage,
            surcharge_start_ordinal: $surcharge_start_ordinal,
        }
    ]
    )
    {
        returning
        {
            id
            name
            price
            capacity
            surcharge_percentage
            surcharge_start_ordinal
            created_at
            updated_at
        }
    }
}
